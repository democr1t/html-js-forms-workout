{
  "type": "module",
  "name": "forms",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc",
    "server": "tsc && node --experimental-specifier-resolution=node dist/index"
  },
  "devDependencies": {
    "@types/node": "^20.5.6",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "zod": "^3.22.2"
  }
}
